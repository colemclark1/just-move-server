class Set {
  constructor(id, number_of_sets, reps, exercise_id) {
    this.id = id;
    this.number_of_sets = number_of_sets;
    this.reps = reps;
    this.exercise_id = exercise_id;
  }
} export default class Set;
